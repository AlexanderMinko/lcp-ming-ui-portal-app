import { Environment } from '../environments/environment';

export const LCP_CUSTOMER = 'lcp_customer';
export const LCP_ADMIN = 'lcp_admin';
export const CREATE_MODE = 'create-mode';
export const UPDATE_MODE = 'update-mode';
export const IMAGE_BASE_URL = Environment.minioUrl;
