export class Environment {
  static production = true;
  static apiUrl = '{API_URL}';
  static keycloakUrl = '{KEYCLOAK_URL}';
  static minioUrl = '{MINIO_URL}';
  // don't use
  static appUrl = '{APP_URL}';
  static accountServiceUrl = '{ACCOUNT_SERVICE_URL}';
  static productServiceUrl = '{PRODUCT_SERVICE_URL}';
  static orderServiceUrl = '{ORDER_SERVICE_URL}';
  static messageServiceUrl = '{MESSAGE_SERVICE_URL}';
}
